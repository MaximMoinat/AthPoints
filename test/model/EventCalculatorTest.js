define(function (require) {

    // Import dependencies.
    var FormulaHelper = require("./EventCalculator");

    // Define the QUnit module and lifecycle.
    QUnit.module("model/EventCalculator", {
        setup: function () {
        },
        teardown: function () {
        }
    });



});